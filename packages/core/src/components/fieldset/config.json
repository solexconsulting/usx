{
  "name": "fieldset",
  "template": "fieldset/fieldset.django.html",
  "props": {
    "legend": { "type": "string", "description": "Text for the fieldset legend" },
    "largeLegend": { "type": "boolean", "description": "Whether to use the larger legend style (adds class `usa-legend--large`)" },
    "required": { "type": "boolean", "description": "Whether to indicate the field is required (adds an asterisk to the legend)" },
    "disabled": { "type": "boolean", "description": "Whether to disable all form controls within the fieldset" },
    "children": { "type": "string", "description": "Inner HTML or text" },
    "className": { "type": "string", "description": "Additional classes" }
  }
}

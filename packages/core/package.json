{
  "name": "@agency-ui-platform/core",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "echo \"Core package build placeholder\"",
    "lint": "eslint --ext .js,.jsx src --ignore-pattern '**/*.stories.jsx' || true",
    "lint:fix": "eslint --ext .js,.jsx src --ignore-pattern '**/*.stories.jsx' --fix || true"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "prop-types": "^15.8.1"
  }
}

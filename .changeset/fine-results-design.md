---
'@solexllc/usx-contracts': patch
'@solexllc/usx-react': patch
---

Banner now relies exclusively on USWDS JS instead of providing its own in react, while also enabling flagSrc param to configure icon in banner
